// Runtime configuration — overwritten by the container entrypoint at startup.
// Values here are development defaults only; never use real credentials here.
window.__RUNTIME_CONFIG__ = {
  apiBaseUrl: '/spacetraders/api',
  dashboardApiKey: '',
}
