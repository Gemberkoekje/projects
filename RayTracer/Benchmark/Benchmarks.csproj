<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BenchmarkDotNet" />
    <PackageReference Include="BenchmarkDotNet.Annotations" />
    <PackageReference Include="Microsoft.VisualStudio.DiagnosticsHub.BenchmarkDotNetDiagnosers" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../RayTracer.Core/RayTracer.csproj" />
    <!-- Benchmarks build maze geometry as a fixture; the maze logic now lives in
         its own net10.0 library (pinball-plan §7.1). -->
    <ProjectReference Include="../RayTracer.Maze.Core/RayTracer.Maze.Core.csproj" />
  </ItemGroup>

</Project>
