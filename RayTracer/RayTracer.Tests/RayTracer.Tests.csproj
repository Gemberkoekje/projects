<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MSTest" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Microsoft.VisualStudio.TestTools.UnitTesting" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../RayTracer.Core/RayTracer.csproj" />
    <!-- Maze unit tests + engine tests that build maze geometry as a fixture
         (pinball-plan §7.1); the maze logic is a plain net10.0 library so the
         test project stays net10.0. -->
    <ProjectReference Include="../RayTracer.Maze.Core/RayTracer.Maze.Core.csproj" />
  </ItemGroup>

</Project>
