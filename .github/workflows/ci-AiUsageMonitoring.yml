name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore ./AiUsageMonitoring/src/AiUsageMonitor/AiUsageMonitor.csproj

      - name: Build
        run: dotnet build ./AiUsageMonitoring/src/AiUsageMonitor/AiUsageMonitor.csproj --no-restore --configuration Release

      - name: Test
        run: dotnet test ./AiUsageMonitoring/tests/AiUsageMonitor.Tests/AiUsageMonitor.Tests.csproj --no-restore --configuration Release

  docker:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./AiUsageMonitoring/src/AiUsageMonitor
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/aiusagemonitor:latest
            ghcr.io/${{ github.repository_owner }}/aiusagemonitor:${{ github.sha }}
